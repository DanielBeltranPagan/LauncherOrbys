<resources>
    <style name="Theme.LauncherOrbys" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Permite que se vea el fondo de pantalla del sistema -->
        <item name="android:windowShowWallpaper">true</item>
        <!-- Hace que las barras de sistema sean transparentes -->
        <item name="windowActionBar">false</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!-- Desactiva las animaciones de ventana para que sea instantáneo -->
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>